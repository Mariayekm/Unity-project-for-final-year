using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Networking;

public class WriteToFilePHP : MonoBehaviour
{
    //private string url = "http://localhost:9000/fromunity.php";

/*    //button creation func
    void OnGUI()
    {
        GUIStyle buttonStyle = new GUIStyle(GUI.skin.button);
        buttonStyle.fontSize = 25;
        if (GUI.Button(new Rect(10,10,450,80), "Send Information to File", buttonStyle))
        {
            StartCoroutine(sendTextToFile());
        }
    }*/

/*    public IEnumerator SendTextToFile()
    {
        bool pass = true;
        WWWForm form = new WWWForm();
        form.AddField("name", "testing");
        form.AddField("age", "32");
        form.AddField("score", "69420");
        UnityWebRequest www = new UnityWebRequest(url, form);
        new WWW();



    }
*/

}
